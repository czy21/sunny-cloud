dependencies {
    flinkShadowJar "org.apache.flink:flink-json:${flinkVersion}"
    flinkShadowJar "org.apache.flink:flink-connector-kafka:${flinkVersion}"
    flinkShadowJar "org.apache.flink:flink-connector-pulsar:${flinkVersion}"
    flinkShadowJar "org.apache.flink:flink-connector-jdbc:${flinkVersion}"
    flinkShadowJar "com.mysql:mysql-connector-j"
}