dependencyManagement {
    generatedPomCustomization {
        enabled = true
    }
    dependencies {
        dependency "com.sunny:sunny-cloud-framework-core:${project.version}"
        dependency "com.sunny:sunny-cloud-framework-db:${project.version}"
        dependency "com.sunny:sunny-cloud-framework-doc:${project.version}"
        dependency "com.sunny:sunny-cloud-framework-file:${project.version}"
        dependency "com.sunny:sunny-cloud-framework-job:${project.version}"
        dependency "com.sunny:sunny-cloud-framework-web:${project.version}"
    }
}