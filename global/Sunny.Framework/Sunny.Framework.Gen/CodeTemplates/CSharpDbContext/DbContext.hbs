{{> dbimports}}

namespace {{namespace}}.Repository
{
    public class {{class}} : DbContext
    {
{{{> dbconstructor}}}
{{{> dbsets}}}

        public static void InitMap(){
            FluentMapper.Initialize(t =>
            {
                {{#each dbsets}}
                t.AddMap(new {{set-property-type}}Map());
                {{/each}}
            });
        }
    }
}
