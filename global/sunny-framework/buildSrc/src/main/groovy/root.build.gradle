tasks.register("clean") {
    group = "build"
    this.subprojects {
        dependsOn ":${it.name}:clean"
    }
}

tasks.register("build") {
    group = "build"
    this.subprojects {
        dependsOn ":${it.name}:build"
    }
}